"""CaveTAK-Weather backend package.

M0.0 scope: foundation scaffolding plus two de-risk spikes — upstream HUC-12
watershed tracing (``cavetak_weather.watershed``) and SREF ensemble extraction
aggregated over a polygon (``cavetak_weather.sref``).
"""

__version__ = "0.0.0"
